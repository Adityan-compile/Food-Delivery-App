import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  heading: {
    fontWeight: 'bold',
    fontSize: 20,
    margin: 10,
    marginBottom: 0,
    fontStyle: 'italic',
    textShadowRadius: 2,
    // color: 'grey',
  },
});

export default styles;
